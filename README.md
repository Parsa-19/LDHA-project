# LDHA-project
this repo is made to gather all repositories which is going to work to gether.</br>
include laravel (on port 8001), django (on port 8002), haproxy which loadbalance them (on port 80) ALL BY DOCKER COMPOSE.

